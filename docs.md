# دليل استخدام إضافة WP YouTube (WP YTB)

مرحباً بك في دليل الاستخدام الشامل لإضافة **WP YouTube Latest**. تم تصميم هذه الإضافة لتكون بسيطة للغاية من حيث التنصيب والاستخدام، وتغنيك تماماً عن التسجيل في Google Cloud أو استخراج مفاتيح API المعقدة، فهي تعتمد بشكل ذكي ومباشر على تقنية الـ RSS الخاصة بيوتيوب.

---

## 1. التثبيت والتفعيل (Installation)

1. بعد تحميل مجلد الإضافة `wp-ytb`، ستقوم بالدخول إلى لوحة تحكم موقعك الووردبريس.
2. انتقل إلى القائمة الجانبية: **إضافات** (Plugins) > **أضف إضافة جديدة** (Add New Plugin).
3. اختر **رفع إضافة** (Upload Plugin) وارفع الملف المضغوط (أو المجلد لو كنت تستخدم FTP) إلى مسار `/wp-content/plugins/`.
4. بعد نجاح الرفع، انقر على **تفعيل الإضافة** (Activate Plugin).

---

## 2. إعداد الإضافة (Configuration)

بعد تفعيل الإضافة، ستظهر لك إعداداتها في مكانين ضمن لوحة التحكم:
1. **ودجت في الرئيسية:** بمجرد دخولك إلى شاشة "الرئيسية" (Dashboard) في ووردبريس، ستجد لوحة توجيهية سريعة تذكرك بطريقة الاستخدام والشورت كود.
2. **صفحة الإعدادات الشاملة:** اذهب إلى **الإعدادات** (Settings) في القائمة الجانبية، ثم اختر **WP YouTube**.

في صفحة الإعدادات العامة يمكنك ضبط التالي:
- **رابط القناة أو الاسم (Handle):** هنا تضع قناتك الافتراضية. يمكنك كتابة رابط القناة كاملاً (مثال: `https://youtube.com/@username`) أو فقط إدخال المعرف (مثال: `@username`).
- **عدد الفيديوهات الافتراضي:** كم عدد الفيديوهات التي تود عرضها كحد أقصى عند طباعة الكود. (أنسب اختيار هو 3، 6، أو 9 فيديوهات نظراً لكون التصميم مقسم إلى 3 أعمدة).
- **تحديث الفيديوهات كل (ساعات):** وقت بقاء الفيديوهات في الذاكرة المؤقتة (Caching). كلما زادت الساعات، كلما قلّ استهلاك مواقعك للموارد. الإعداد الافتراضي هو `12` ساعة ممتازة جداً.

---

## 3. النماذج والتصاميم المتاحة (Templates)

توفر الإضافة الآن 3 نماذج عرض مختلفة يمكنك التبديل بينها من **إعدادات التصميم (Layout)**:

1. **نموذج البطاقة (Standard Card):** التصميم الكلاسيكي الأنيق (صورة في الأعلى ونصوص في الأسفل).
2. **نموذج القائمة (Modern List):** تصميم أفقي (الصورة على اليمين والنصوص على اليسار) - مناسب جداً للأعمدة الجانبية أو القوائم الطويلة.
3. **نموذج التراكب (Overlay Card):** تصميم عصري يتم فيه كتابة العنوان فوق الصورة مباشرة مع تدريج لوني (Gradient) لضمان القراءة.

> [!TIP]
> يمكنك تغيير النموذج لكل شورت كود على حدة باستخدام السمة `template` أو تغييره بشكل عام من لوحة الإعدادات.

---

## 3. طريقة الاستخدام والشورت كود (Usage & Shortcodes)

الإضافة تعتمد بشكل كامل على الأكواد المختصرة (Shortcodes). يمكنك استخدام هذا الكود في المحرر العادي (Gutenberg)، والمحرر الكلاسيكي، وحتى في باني الصفحات مثل Elementor.

### الاستخدام البسيط (الافتراضي)
انسخ الكود التالي والصقه أينما تريد عرض الفيديوهات:
```text
[youtube_latest]
```
> *هذا الكود سيقرأ الإعدادات التي قمت بضبطها في صفحة الإعدادات تلقائياً (القناة والعدد).*

### الاستخدام المتقدم (لأكثر من قناة)
ماذا لو كان لديك عدد من المؤلفين في الموقع وتريد إظهار الفيديوهات من قنوات يوتيوب مختلفة لكل كاتب في صفحته؟ تستطيع بكل سهولة استخدام المتغيرات `channel` و `limit` لتجاوز الإعدادات الافتراضية:

```text
[youtube_latest channel="@Apple" limit="3" template="list"]
```
- `channel`: يقبل اليوزر أو رابط القناة المباشر.
- `limit`: يقبل عدد الفيديوهات الذي تود جلبه.
- `template`: يقبل القيم `card` أو `list` أو `overlay`.

---

## 4. التحكم في التخزين المؤقت وتحديث البيانات (Cache Management)

من أهم مميزات الإضافة أنها **لا تقوم بالاتصال بيوتيوب مع كل زيارة لموقعك** تجنباً لبطء الموقع. بل تتصل مرة واحدة، وتخزن الفيديوهات (Cache) للمدة التي حددتها (مثلاً 12 ساعة). 

**كيف أجُبر الموقع على عرض فيديو جديد نشرته للتو؟**
الأمر بسيط. لا تحتاج لانتظار الـ 12 ساعة، يمكنك:
1. التوجه لـ **الإعدادات > WP YouTube**.
2. النزول لأسفل الصفحة والضغط على زر **إفراغ الكاش (Clear Cache)**.
3. بمجرد تأكيد الإجراء، ستقوم الإضافة بحذف الملفات المؤقتة، ومع أول زيارة للصفحة سيتم الاتصال المباشر بيوتيوب وجلب الفيديوهات المحدثة.

---

## 5. تخصيص الألوان والهوية (Color Customization)

يمكنك الآن التحكم الكامل في ألوان العناصر من تبويب **تخصيص النصوص (Typography)** دون الحاجة لكتابة كود:

- **لون العناوين:** التحكم في لون عنوان الفيديو.
- **لون التاريخ:** لتغيير لون تاريخ النشر.
- **خلفية البطاقة:** تغيير لون خلفية الكارد بالكامل.
- **خلفية المحتوى:** لون منطقة النص (مفيد لفصلها عن منطقة الصورة).
- **ألوان أيقونة التشغيل:** يمكنك تغيير لون السهم ولون خلفية الدائرة (تدعم شفافية RGBA).

---

## 5. دليل المطورين وتخصيص التصميم (Developer Reference)

تم تصميم الإضافة لتكون مرنة للغاية وقابلة للتخصيص من قبل المطورين. يمكنك استخدام نظام الكلاسات (CSS Classes) أو المحرر المدمج في لوحة التحكم لتغيير المظهر بالكامل.

### 🔹 مراجع الكلاسات (CSS Classes Reference)

يمكنك استهداف العناصر التالية لتخصيص التصميم بدقة:

- **Grid Container:**
  - `.wp-ytb-grid` : الحاوية الرئيسية لشبكة الفيديوهات. هي المسؤولة عن الـ Grid Layout والمسافات (Gap).
- **Video Card:**
  - `.wp-ytb-item` : الكارد (Card) الكامل لكل فيديو. يشمل الصورة والعنوان والتاريخ.
- **Thumbnail Section:**
  - `.wp-ytb-thumb` : حاوية الصورة المصغرة.
  - `.wp-ytb-play-icon` : أيقونة التشغيل (Play) التي تظهر عند تمرير الماوس.
- **Content Section:**
  - `.wp-ytb-content` : الحاوية التي تضم النصوص (العنوان والتاريخ).
  - `.wp-ytb-title` : عنوان الفيديو.
  - `.wp-ytb-date` : تاريخ النشر.

### 🔹 أمثلة على التخصيص (Custom CSS Examples)

يمكنك إضافة هذه الأكواد في **الإعدادات > WP YouTube > إعدادات متقدمة > Custom CSS Box**:

**1. إضافة إطار غامق وحواف حادة للفيديوهات:**
```css
.wp-ytb-item {
    border: 1px solid #333;
    border-radius: 0px !important;
    box-shadow: none !important;
}
```

**2. تغيير لون أيقونة التشغيل وخلفيتها:**
```css
.wp-ytb-play-icon {
    background: rgba(255, 0, 0, 0.8) !important;
}
.wp-ytb-play-icon svg {
    fill: #fff !important;
}
```

**3. إخفاء تاريخ النشر يدوياً بـ CSS:**
```css
.wp-ytb-date {
    display: none !important;
}
```

### ⚠️ ملاحظات هامة للمطورين:
- الإضافة تستخدم متغبرات CSS (Variables) مثل `--ytb-gap-desk` و `--ytb-radius`. يمكنك محاولة تعديلها مباشرة.
- لتجاوز الإعدادات الافتراضية بنجاح، قد تحتاج لاستخدام `!important` في أكوادك المخصصة لأن الإضافة تستخدمه لضمان استقرار التصميم ضد تداخلات القوالب (Themes Overrides).
- تأكد من عدم حذف الكلاسات الأساسية لأنها مرتبطة بالـ Layout المتجاوب (Responsive).

---

نتمنى لك تجربة ممتعة في استخدام وتطوير الإضافة! 🚀
